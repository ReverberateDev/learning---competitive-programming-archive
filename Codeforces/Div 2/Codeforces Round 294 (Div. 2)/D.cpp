#include <bits/stdc++.h>
using namespace std;
#define int long long
#define double long double
#define DEBUG 1

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int val[26];
    for(int i = 0; i < 26; i++){
        cin >> val[i];
    }
    string s; cin >> s;
    int sz = (int)s.length();
    for(int i = 0; i < )
}